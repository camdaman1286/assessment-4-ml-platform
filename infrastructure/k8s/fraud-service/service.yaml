apiVersion: v1
kind: Service
metadata:
  name: fraud-service
  namespace: ml-platform
  labels:
    app: fraud-service
spec:
  selector:
    app: fraud-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
