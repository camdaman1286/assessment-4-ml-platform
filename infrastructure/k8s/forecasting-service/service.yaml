apiVersion: v1
kind: Service
metadata:
  name: forecasting-service
  namespace: ml-platform
  labels:
    app: forecasting-service
spec:
  selector:
    app: forecasting-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
